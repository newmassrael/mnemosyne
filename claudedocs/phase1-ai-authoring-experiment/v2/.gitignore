# Scratch stores stay out (the experiment convention, R404 seeded-fixture rule):
# the authored sections.json/facts.json/order.json reproduce the store via
# import-sections/import-facts. Tracked evidence = manifest, premise, the author
# + judge briefs, runbook, the authored JSON sources, the per-world manuscripts,
# author-log.md, and report.md with the deterministic gate verdicts inlined.
*.atomic.json
*.playthrough.json
