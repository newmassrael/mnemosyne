# time-travel-experiment/v2 — report (SSOT)

**Outcome: PIN-M1 HOLDS; MYSTERY wins the A/B 3-0.** A controlled re-render of v1's
FROZEN base in a Withering-FIRST (mystery) discourse passes the leak gate (leak=0)
that v1's Founding-first IRONY render FAILED (leak=1) — the SAME base, ONLY the
reading order changed. And 3 blind A/B judges unanimously found the mystery telling
the better time-travel game. The R571 disclosure-order blemish is EXECUTABLE-CLOSED:
future-first is EXPRESSIBLE on the EXISTING substrate via render discipline, and the
discourse order demonstrably MATTERS for craft. The leak gate that flagged v1 was
tracking a real weakness, not over-strictness.

Manifest sha256 `7f42c24b7720539d0d527c7f702b60b531c0f1a04cbbe17f574a3ac96dfb66cb`
(pinned in the R572 ledger pre-execution). Firewall = R469 (14th use): the R572
lineage ran the rebuild/outline/gates/A-B assembly only; blind subagents re-rendered,
re-extracted, and judged. NO new author — v1's frozen gate-clean base reused (R553
controlled-re-render; the frozen-ledger rule). Only the discourse order is the
variable.

## PIN-M1 — the Withering-first MYSTERY render is leak-CLEAN: HOLD

On the orchestrator's fresh rebuild of v1's frozen base (65 facts):
- **MYSTERY leak gate**: `validate-disclosure-leak --telling wend --world planted
  --truth-frame gt` → **leaks = 0**, vocabulary_shared = 15 (>0, the R510 F5
  non-vacuous guard passes), truth_frame_typed = 15. No withheld Founding cause is
  disclosed before its pin.
- **MYSTERY fidelity**: `validate-render-fidelity --world planted` → off_path = 0,
  unplaced = 0, reached_terminal = true. The mystery render stays on the planted
  road and reaches its ending.
- **IRONY re-confirm** (same base, same gate): leak = 1 (`f-seal-vent` @ sc-04, the
  R571 result reproduced exactly).
- **The contrast**: the SAME frozen facts + disclosure plan + world-line, with ONLY
  the discourse order changed, flips **leak 1 → 0**.

The blind re-extractor (independent re-read of the mystery prose) confirmed the
mechanism on the page: both Founding causes (the cistern seal, the ford marking) are
first stated as ground truth ONLY at sc-39p; the Withering scenes (sc-27p..sc-38p)
render the clean water and firm crossing as unexplained wonders with explicit
not-knowing (Marn "has no story for it"; the folk "called it luck"). 32 re-extracted
facts, import clean.

## Why it passes (the design resolution, verified in code)

The leak gate (`disclosure.rs:211-278`) reads each withheld fact's re-extracted
`canon_from` — the prose's OWN `## sc-NN` heading — and flags a typed-tuple match
at-or-before the pin in canon order. The mystery render never states a withheld
Founding cause under any heading before sc-39p (the pin), so the gate finds the
cause first disclosed AT the pin → leak 0. The Founding age is rendered AT the
reveal (sc-39p), so the re-extractor records the causes there. This is the existing
substrate honoring a discourse-order reveal — no new axis needed.

## Judged A/B — MYSTERY wins 3-0 (blind, positions varied per judge)

Arm IRONY = v1's render (Founding trunk + planted Withering, 4,649 words, leak 1).
Arm MYSTERY = v2's render (Withering-first planted, 3,628 words, leak 0). Decoded
against the label map:

| Judge | Reveal impact | Era texture | Time-travel coherence | Overall | Winner |
|---|---|---|---|---|---|
| J1 | irony 2 / MYSTERY 5 | IRONY 5 / mystery 2 | irony 5 / mystery 4 | irony 4 / MYSTERY 5 | MYSTERY |
| J2 | MYSTERY 5 / irony 2 | IRONY 5 / mystery 3 | mystery 4 / irony 5 | MYSTERY 5 / irony 3 | MYSTERY |
| J3 | irony 2 / MYSTERY 5 | IRONY 5 / mystery 3 | mystery 5 / irony 5 | irony 3 / MYSTERY 5 | MYSTERY |

- **MYSTERY wins overall 3-0** and **reveal-impact 3-0** (5 vs 2 every time).
- **IRONY wins era-texture 3-0** (5 vs 2-3) — exactly the preregistered trade.
- All three independently named the SAME reason, unprompted: IRONY "shows the seal
  (sc-04) and stone (sc-11) up front, so the reveal only RESTATES what the reader
  already saw"; MYSTERY "withholds until the cross, so the why lands as EARNED
  DISCOVERY." Each also named IRONY's real strength: the fully-lived Founding-Age
  present (which MYSTERY only glimpses at the reveal).

This is the load-bearing finding: **the mechanical leak gate's flag on v1 was not
over-strictness — it was tracking a real craft weakness the readers also feel.** A
gate-clean reveal order and the better-reading reveal order COINCIDE here. (Contrast
R500's fidelity != craft: here the disclosure gate and craft AGREE.)

## Decision (pre-committed rule: mystery_leak_clean_and_judged)

PIN-M1 holds (MYSTERY leak 0 + fidelity clean; IRONY re-confirmed leak 1) AND the
A/B is judged (MYSTERY 3-0 overall) ⇒ the R571 blemish is EXECUTABLE-CLOSED: a
Withering-first discourse yields a genuine reader-mystery reveal that passes the
gate on the EXISTING substrate AND reads as the better time-travel game. Future-first
is EXPRESSIBLE today (no substrate change).

## The (가)→(나) decision input

PIN-M1 PASS ⇒ future-first is EXPRESSIBLE on the existing substrate via render
discipline. So the (나) question narrows from "is it expressible?" to "should a
first-class discourse-order axis be ENFORCED?" — and the (가) evidence leans strongly
toward NO NEW SUBSTRATE:

- The leak gate ALREADY enforces disclosure-order coherence: it FLAGGED v1's
  wrong-order render (leak 1) and PASSED v2's right-order render (leak 0). The gate
  IS the discourse-order check.
- What was missing in v1 was render DISCIPLINE (heed the gate; treat leak>0 as "your
  reveal order is wrong"), NOT a substrate feature.
- A positive "declare the telling is Withering-first" axis would be redundant — the
  leak gate catches the symptom (early disclosure) without a declaration.

So the (나) round's likely conclusion is a refute-first NO-ADD (the existing leak
gate suffices as the enforcer; the fix is authoring/render discipline). Recorded as
the R573 finding for the (나) round to formalize or refute.

## Honest residue (not downplayed)

1. **MYSTERY loses era-texture 3-0** — the genuine cost, named pre-execution. The
   Founding age is vivid in IRONY (lived in full) and thin in MYSTERY (glimpsed only
   at the reveal). The gate-clean mystery form is NOT strictly better; it TRADES the
   lived-in past for the earned reveal. A full game might want BOTH — show the
   Founding age AND preserve a reveal — which would need a base where the bearer's
   first Founding visit does not disclose the CAUSAL significance (a craft/structure
   design, not tested here).
2. **n=1** controlled re-render on ONE base; the discourse-order variable only.
3. The A/B is structurally tilted on era-texture (IRONY renders the Founding trunk;
   MYSTERY does not) — named pre-execution; the A/B measured the genuine trade.
4. The bridge-tree's planting cause (sc-26) is shown in neither arm's reveal (the
   re-extractor flagged it as an uncaused wonder) — a minor slice-scope gap.

## NO Mnemosyne build

Substrate sufficient (the leak gate already enforces disclosure order; future-first
is expressible). The (나) round will formalize the no-add lean. The live craft note:
a time-travel reader-mystery reveal needs Withering-first discourse, and the leak
gate is the tool that tells you when you got it wrong.
