# time-travel-experiment/v1 — report (SSOT)

**Outcome: REACHES.** A blind AI self-authored a real TIME-AXIS game — the same
place (Brae Hollow) in a Founding Age and a Withering Age, both canonical, with
early acts causally changing the late age — on the EXISTING substrate. All five
deterministic pins HOLD (PIN-3/PIN-4 decisively, via fail-loud negative controls);
the R570 substrate resolution (ERA = canon-order coordinate, ZERO new substrate) is
CONFIRMED on self-authored data; 3 blind judges scored it UNANIMOUS 5/5 on every
axis and all called it "a real time-travel game." One honest deterministic blemish:
a disclosure-plan / reading-order incoherence (below).

Manifest sha256 `e6d1ab20190756d5638bbaee9233819191a45dc5514d3a80c3d5728ea520b53a`
(pinned in the R570 ledger pre-execution). Firewall = R469 (13th use): the R570
lineage wrote the manifest/premise/briefs/runbook and ran the gates/scans/negative-
controls/slice/briefing ONLY; blind subagents authored, rendered, re-extracted, and
judged. All pin verdicts below were computed by the orchestrator on a FRESH rebuild
from the author's JSON (self-report not trusted, the R500 discipline).

## The base (blind author)

54 scenes. Founding-Age band `sc-01..sc-26` (shared spine), Withering-Age band
`sc-27p..sc-40p` (planted) + `sc-27b..sc-40b` (barren). 65 facts, 8 frames (gt +
sera/halden/bearer in the Founding age, ode/marn/pell/tamsin in the Withering age),
2 branches (the one planting fork at sc-26), 16 entities, 2 predicates
(condition/possession). 2 author write→gate→repair iterations (iteration 1's only
flag = payoff-substantiation wanting a real state transition, fixed by typing the
chains rotting→sealed→clean / unmarked→marked→crossed / planted→bridged with
`supersedes_in_frame`).

## PIN-1 — authoring gate-clean: HOLD

On the orchestrator's fresh rebuild (`import-sections` + `import-facts` = 0 errors,
65 facts):
- `validate-continuity --order --rules`: violations 0 (structural 0, interval 0,
  conflict_pairs 0, cross_scope 0, unordered 0, unchained_state_pairs 0, the
  exclusive `condition` rule + succession clean).
- `report-fork-tree`: 2 world-lines, 0 unplaced fork points, fork at sc-26, both
  reach terminals.
- `report-playthrough-manuscript --world {planted,barren}`: unplaced 0, undecidable 0.
- `report-timeline-gaps --world {planted,barren}`: violated 0, unverifiable 0.
- `report-payoff-coverage`: planted paid 4 / dangling 0; barren paid 3 / dangling 0;
  `main` dangling 3 (f-keeper-mark, f-seal-vent, f-set-stone) = the correct R442
  inherited-setup shape (a Founding setup is paid in the Withering band post-fork,
  so it "dangles" on the pre-fork spine — not a hole). The barren far-field is open
  by design (no planting on that road).

## PIN-2 — era = an ORDER coordinate (not a frame, not a branch): HOLD

The headline (operationalizes the R570 resolution):
- **9 shared-place Entities** referenced by BOTH a Founding-Age and a Withering-Age
  fact on the same world-line: cistern-steps, loom-quarter, wend-fields, cistern,
  cistern-vent, ford, boundary-stone, gorge-sapling, bearer-person (≥2 required).
- **Same prop object resolves across ages** (report-playable-world): `cistern-vent`
  → MapLocator at sc-06 (Founding) AND sc-31p (Withering); `boundary-stone` → sc-12
  (Founding) AND sc-35p (Withering); `gorge-sapling` → sc-20 (Founding) AND sc-37p
  (Withering). 0 unresolved, 0 unplaced, 0 undecidable on every world.
- **Canon order past < future**: on both roads the last Founding scene (index 25)
  is strictly before the first Withering scene (index 26) — the eras are an ORDER
  band, not interleaved and not a fork between them.
- **R442 cross-time payoff**: 7 Founding-setup → Withering-payoff edges.
- **R547 succession across the age gap**: f-clean-water-{p,b} (sc-31) supersedes
  f-seal-vent (sc-04); the Founding state succeeds into the Withering state on the
  same object. The eras differ only in canon-order position (and POV frame per
  character — the orthogonal R541 axis), not as two branches or two frames.

## PIN-3 — past→future causation is GATED: HOLD (decisively)

Negative control: removed the Founding setup `f-seal-vent`, rebuilt fresh. The
rebuild **fails loud at the mutate gate**: `fact f-clean-water-p: pays_off
f-seal-vent not present (a payoff resolves an existing setup; fail-loud)`. The
future clean-water cannot exist without its past cause. The error traces EXACTLY to
the removed fact (surgical). This is a STRONGER outcome than the manifest's
predicted dangling-rescan — the `pays_off` referential invariant fires at import.
The past→future link is structural, not prose-only.

## PIN-4 — multi-character × cross-region causation is GATED: HOLD (decisively)

The owner-added axis. 7 person-frames across 3 regions; halden acts only in the
Loom Quarter (Founding), ode only in the Wend Fields (Withering). The load-bearing
cross-corner chain: `f-set-stone` (gt setup, Loom Quarter, Founding) is paid off by
`f-ode-crosses-{p,b}` (ode's frame, Wend Fields, Withering) — cross-era AND
cross-region AND cross-frame (R442 "payoff edges cross FRAMES freely but never
WORLDS"). Negative control: removed `f-set-stone`, rebuilt fresh → fails loud:
`fact f-ode-crosses-p: pays_off f-set-stone not present (fail-loud)`. Ode's future
crossing in the Wend Fields cannot exist without Halden's past act in the Loom
Quarter, three generations earlier. One act in one corner reaching another person
in another corner across the years is STRUCTURAL, gate-enforced. (Honest note: the
structural setup is gt-framed — correct modeling, since causation is ground truth;
the two CHARACTERS halden/ode sit at the chain's endpoints in their own belief
frames in different regions/eras. The cross-frame edge is gt→ode.)

## PIN-5 — optional fork, per-era state: HOLD

The planting choice (sc-26) is the single fork into 2 Withering futures, both
terminal. The far-field obligation is DISCHARGED in `planted` (f-plant-sapling ←
f-bridge-tree-p) and OPEN in `barren` (the planting thread absent by design) — the
"state DERIVED per world-line" claim on the time axis. The author registered NO
`kind=quest` entity (report-quest-graph: 0 quests), which the manifest permits;
PIN-5 rests on the report-payoff-coverage per-world divergence, which holds.

## Render-acceptance gate (the slice: Founding trunk + planted future, 4,649 words)

- **Fidelity: PASS** — `validate-render-fidelity --world planted`: off_path 0,
  unplaced 0, reached_terminal true. The warm render did not drift off the road.
- **Leak: 1** — `validate-disclosure-leak --world planted --truth-frame gt`:
  leaks 1, vocabulary_shared 8 (>0, the R510 F5 non-vacuous guard passes),
  truth_frame_typed 8. The lone leak: `f-seal-vent` (withheld, first_at sc-39p) is
  disclosed at **sc-04** — the Founding trunk scene where Sera actually seals the
  vent. `f-set-stone` did NOT leak (the re-extractor typed the ford as
  `boundary-stone possession ford`, a different tuple than f-set-stone's).

### The leak finding (NOT downplayed — feedback_dont_downplay_experiment_flaws)

This is a genuine **disclosure-plan / reading-order incoherence**, the time-axis
analog of store-consistency ≠ causal-coherence (here: store-consistency ≠
disclosure-coherence). It is NOT a render fault and NOT a structural-pin failure:

- The blind render correctly held the cause from the late-age CHARACTERS — the
  independent re-extractor confirmed "no late-age character voices a Founding cause
  the text had not already shown"; Marn (sc-29p) and Ode (sc-35p) explicitly
  disclaim knowing the cause; the causal "because" is named only at sc-39p.
- The leak is at sc-04, in the Founding trunk, where the cause-EVENT genuinely
  occurs. A `withhold` on a Founding cause cannot coexist with rendering that
  cause-event in an earlier era of the slice. Diagnostic: setting first_at to sc-04
  STILL leaks — because the event is shown at sc-04, no Founding-first reading can
  make the cause "first disclosed" later.
- Root cause: the author wrote a Founding-FIRST canon (the bearer arrives young and
  witnesses the acts) but a withhold-the-cause-until-the-cross disclosure plan. The
  two are incompatible: a reader-MYSTERY time-travel reveal needs a Withering-FIRST
  discourse; a Founding-first reading yields DRAMATIC IRONY (the reader sees the
  cause; the late-age characters don't), which is a coherent form but is NOT what
  `withhold` encodes.
- **Gate vs felt-craft divergence**: the mechanical leak gate (any truth-frame
  disclosure of a withheld tuple before first_at) flags this; the 3 blind judges
  (reader experience) did NOT — all read the reveal as landing at sc-39p. The
  disclosure gate is stricter than the reader's felt reveal. Recorded as the honest
  blemish, blind-judged as not harming the read.
- Repair count: 0 RENDER repairs (the prose is correct). The fix is an AUTHORING
  disclosure-plan/reading-order correction (Withering-first discourse, or drop the
  withhold and play it as dramatic irony), recorded not applied (frozen-experiment
  + the prose already reads as a coherent reveal to the judges).

## Judged (the open question) — 3 blind judges, UNANIMOUS 5/5

All three, on the cross-age briefing + the rendered slice (blind to each other, the
pins, and the AI-authorship):

| Axis | J1 | J2 | J3 |
|---|---|---|---|
| Era consistency | 5 | 5 | 5 |
| Causal-payoff satisfaction | 5 | 5 | 5 |
| Cross-region coherence | 5 | 5 | 5 |
| Time-travel legibility | 5 | 5 | 5 |
| Overall time-travel game | 5 | 5 | 5 |

Verdict line (all 3): **"a real time-travel game"** — two ages of one place that
lock together "stone-by-stone," the two hidden Founding causes paying off as earned
Withering consequences across both time and corners, the reveal withheld then
witnessed at sc-39p. Each independently named the SAME soft spot: the barren
future is only NAMED in the briefing, not SHOWN as prose (the render slice covered
the planted road only); two also noted the planting fork's outcome is weighed
rather than witnessed-completed. These are slice-scope limits, not coherence breaks.

## Decision (pre-committed rule: all_pins_hold_and_judged_good)

PIN-1..PIN-5 all HOLD (PIN-3/PIN-4 decisively via fail-loud negative controls) AND
judges unanimous 5/5 (≥4 majority) on era-consistency, causal-payoff, and overall,
with no era/causation break ⇒ **the rule FIRES**: an AI self-authored a real
TIME-AXIS game on ZERO new substrate, and it reads as coherent time travel. The
R570 resolution (era = order coordinate; same-place = shared entity + cross-era
MapLocator; cross-era/cross-region causation = R442 cross-frame payoff + R547
succession; toggle = navigation) is CONFIRMED on self-authored data.

## Honest residue (not downplayed)

1. The disclosure-plan / reading-order incoherence above (the 1 leak): the strongest
   honest finding — a blind author handed a time-travel reveal can write a
   gate-clean, judge-loved DRAMATIC-IRONY structure while authoring a `withhold`
   plan inconsistent with the Founding-first reading. A reader-mystery reveal would
   need Withering-first discourse. Store-consistency ≠ disclosure-coherence on the
   time axis. The leak gate caught it; the judges did not.
2. The render is a SLICE (Founding trunk + planted future) — the judges read one
   road's prose; the barren future is inferred from the briefing.
3. The negative controls prove STRUCTURAL dependency (remove cause → effect can't
   exist), not in-world SUFFICIENCY (the R476 commonsense ceiling, judged not gated).
4. SIMULTANEOUS state-COMBINATION (AND-join) remains OUT of scope (R549 unbuilt L3,
   pull-gated). This covered TIME-LAGGED cross-region causation only.
5. n=1 (1 premise / 1 author / 1 render / 1 extractor / 3 judges).

## Tooling pull (orthogonal to pass/fail)

The PIN-1 gates + the PIN-3/PIN-4 negative controls + the render gates are all
production mnemosyne-cli verbs (reproducible, no harness). The PIN-2 era-as-order
JOIN (shared-place-entities-in-both-ages + the canon-order band check) was an
ad-hoc python scan over facts.json — the MEASURED PULL for a `report-era-band` /
per-era projection (the R570-predicted pull). For n=1 the pull is real but modest;
build deferred (YAGNI, the R556→R557 design-then-build-when-pulled discipline). The
cross-age briefing hand-JOIN was light (the existing reports carried most of it).

## NO Mnemosyne build

Substrate sufficient (the R547/R556 refrain confirmed on the time axis). The one
finding that points anywhere is the disclosure-plan/reading-order incoherence — a
DISCOURSE-ORDER authoring concern (the syuzhet layer), addressable in the brief/
render discipline (render time-travel reveals Withering-first, or type the reveal
as dramatic irony), not a substrate gap. n=1; no pull pursued.
