{
  "schema": "narrative-rules/v1",
  "comment": "Ondree, a flooded hill-town. The map is the `adjacent` facts; the quarters are the `contains` facts.",
  "rules": [
    {
      "id": "one-place-per-person",
      "predicate": "at",
      "class": "exclusive",
      "per": "subject",
      "containment": "contains"
    },
    {
      "id": "one-holder-per-thing",
      "predicate": "holds",
      "class": "exclusive",
      "per": "object"
    },
    {
      "id": "town-map",
      "predicate": "at",
      "class": "transition",
      "adjacency": "adjacent",
      "undirected": false,
      "containment": "contains"
    }
  ]
}
