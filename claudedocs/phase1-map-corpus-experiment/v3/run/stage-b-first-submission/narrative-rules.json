{
  "schema": "narrative-rules/v1",
  "comment": "Kelder flood: the map is the store. `adjacent` facts are the edges (one fact = one direction), `contains` facts partition them into scopes (the Crown and the Tidegate quarters, plus the root scope of causeway / water stair / cistern / drowned bell / the two quarters themselves).",
  "rules": [
    {
      "id": "one-place-per-person",
      "predicate": "at",
      "class": "exclusive",
      "per": "subject",
      "containment": "contains"
    },
    {
      "id": "one-keeper-per-thing",
      "predicate": "holds",
      "class": "exclusive",
      "per": "object"
    },
    {
      "id": "movement-follows-the-map",
      "predicate": "at",
      "class": "transition",
      "adjacency": "adjacent",
      "containment": "contains"
    }
  ]
}
