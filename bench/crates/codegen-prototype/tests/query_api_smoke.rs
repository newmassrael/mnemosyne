//! Round 252 — query_api_smoke disabled.
//!
//! The original tests indexed parsed workspace docs from the now-deleted
//! 7-md sources (Round 251 MD-DELETION-RATIFY). Re-anchoring them against
//! the atomic store is deferred to a follow-up bench round.

#[test]
#[ignore = "Round 252 -- 7-md fixtures deleted; bench prototype query smoke pending re-anchor"]
fn query_api_smoke_placeholder() {}
